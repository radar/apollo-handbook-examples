overwrite: true
schema: "https://apollo-handbook-api.vercel.app/api/graphql"
documents: "components/**/*.{tsx,ts}"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
